/*A notação de colchete é uma maneira de obter um caractere em um 
específico index dentro de uma string.
Por exemplo, o caractere no índice 0 na palavra "Maria" é "M".

Armazene o nome "Regina" em uma variável e use a notação de colchete
para encontrar e exibir o primeiro caractere do nome armazenado.*/

var nome = "Regina";
var primeiraletra = nome[0];
console.log(primeiraletra);

var segundaletra = nome[1];
console.log(segundaletra);
